# 3D Viewer — веб-инструмент для чертежей и 3D

Веб-приложение для просмотра и согласования **PDF-чертежей** и **3D-моделей** в браузере: получателю не нужны AutoCAD, КОМПАС-3D и т.п. Поддерживаются **аннотированные скриншоты**, **PDF-отчёт** с измерениями, **мультимодельная сборка** с сохраняемыми сопряжениями по плоскостям, отдельные деревья файлов **PDF/3D** и облегчённый режим **«Производство (QR)»**.

**Версия фронта:** 1.6.0 (см. `viewer/package.json`). В этой версии: независимые деревья **PDF** и **3D** в левой панели, единые хедеры 2D/3D и упрощённый режим **«Производство (QR)»** для мобильных ссылок на чертежи.

---

## Зачем это нужно

На рынке обычно разделены:

- **PDF-редакторы** — не заточены под инженерный цикл «открыл чертёж → отметил на 3D → в отчёт»;
- **CAD-просмотрщики** — отдельные файлы и лицензии, редко единый сценарий «ссылка в чат → браузер → скриншот с подписью → один PDF».

Этот проект — **«инженерный мост»** в одной странице:

| Возможность | Описание |
|-------------|----------|
| **PDF + 3D** | Одна вкладка: чертёж и модель рядом. |
| **Скриншоты и отчёт** | Редактор фигур/текста, сборка **одного PDF** (jspdf), блок измерений. |
| **Сборка** | До нескольких моделей в сцене, **связи по плоскостям** (совпадение, расстояние, симметрия по ширине), повторное применение после перетаскивания. |
| **2D-first сценарий** | PDF и 3D выбираются независимо, без обязательной автопривязки. |

3D — **усиление** к PDF, а не обязательный слой: фронт можно запускать **без сервера** (тогда нет серверной конвертации STEP по API; STEP всё равно можно грузить через **WASM** в браузере в пределах лимита размера).

---

## Кому полезно

| Роль | Задача |
|------|--------|
| **Конструкторы, проектировщики** | Показать узел, сделать скриншот с подписью, собрать **отчёт-PDF**, зафиксировать **сопряжения** в сборке. |
| **Производство, монтаж, заказчик** | Открыть ссылку, увидеть **PDF** и при необходимости **3D** без CAD. |
| **Команды в мессенджерах** | Одна страница вместо цепочки «скинь файл» — дальше скриншоты и ссылки. |

---

## Возможности 3D-сцены (`Viewer3D.vue`)

### Форматы и загрузка

- **STL**, **STEP** (`.stp`, `.step`), **IGES** (`.igs`, `.iges`), **GLB/GLTF**.
- **STEP/IGES:** сначала запрос **серверной** конвертации (CadQuery + trimesh → GLB); при недоступности или ошибке — **opencascade.js (WASM)** в браузере.
- **Лимит STEP/IGES на фронте:** **30 МБ** (больше — загрузка не выполняется, сообщение в лог).
- На сервере отдельный лимит для конвертации — см. [server/README.md](server/README.md).

### Навигация и вид

- **TrackballControls**, меню **ориентаций**, режим **«Перпендикулярно к грани»** (клик по грани + кнопка).
- **Сетка пола** (вкл/выкл), размер от габаритов модели.
- Во время вращения камеры — снижение `pixelRatio` и throttling тяжёлых расчётов (hover и т.д.).

### Материалы и освещение

- Режим **«С освещением» (lit):** **MeshPhongMaterial**, направленные источники + ambient/hemisphere; **карты теней** (мягкие PCF) от ключевого света, подстройка теневой камеры под **габариты сцены**; баланс fill-света, чтобы тени читались на **светлой** модели.
- Режим **«Без шейдинга» (unlit):** уплощённый вид (Lambert + emissive), **тени отключены** для скорости и предсказуемого вида.
- **Оттенок / яркость модели** — быстрый слайдер без смены типа материала.
- **Каркас:** не проволока, а **полупрозрачные грани**; прозрачность 0.05–0.95 (поле, слайдер, колёсико).

### Сечение и измерения

- **Сечение:** выбор оси/смещение, клик по грани для ориентации плоскости, фиксация/снятие.
- **Измерения:** два клика, привязка (грань, вершина, ребро и др.); длина и ΔX/ΔY/ΔZ в мм.
- **Журнал измерений** — таблица поверх канваса: восстановление по клику, удаление строки, столбцы для перпендикуляра/треугольника где применимо.

### Сборка (несколько моделей)

- Выбор **источника** и **опорной** модели из списка загруженных **в сцене**.
- Типы сопряжений: **плоскость ↔ плоскость**, **по расстоянию**, **симметрия по ширине** (четыре плоскости: две на базе, две на детали).
- **Пошаговый выбор граней** подсвечивается в сцене (треугольник грани или маркер плоскости).
- После **«Применить сопряжение»** связь сохраняется в таблице; при **перетаскивании** детали связи **пересчитываются**.
- Таблица **«Зафиксированные связи»:** клик по строке подсвечивает плоскости этой связи в 3D; повторный клик снимает подсветку; удаление связи — кнопка в строке.

### Прочее в 3D

- **Метрики сцены:** площадь поверхности, объём, число треугольников (для моделей в сцене).
- **Экспорт:** GLB, STL.

### PDF (вкладка рядом с 3D)

- Просмотр через **PDF.js**.
- Скриншоты области 2D/3D, редактор аннотаций, включение в **итоговый PDF** с шапкой и блоком измерений.

### Производство (QR)

- Отдельный режим интерфейса для мобильного сценария.
- Заготовка страницы модуля с QR и списком ссылок на чертежи из дерева PDF.

### Логирование

- Режимы: **полный** (debug), **обычный** (info), **тихий** (warn+error).

---

## Стек технологий

| Слой | Технологии |
|------|------------|
| Фронт | **Vue 3**, **TypeScript**, **Vite**, **Three.js** |
| STEP в браузере | **opencascade.js** (WASM) |
| Бэкенд | **FastAPI** — метаданные STEP, конвертация STEP→GLB, спецификация |
| PDF | **PDF.js**, **jspdf**, **jspdf-autotable** |

---

## Запуск

### Всё сразу (Windows)

Из корня репозитория:

```batch
run.bat
```

Ожидаемо: сервер на **:8000**, Vite на **http://localhost:5173** (два процесса/окна — см. ваш `run.bat`).

### Только фронт

```bash
cd viewer
npm install
npm run dev
```

Без сервера: нет серверной конвертации STEP и нет данных спецификации с API; STEP до лимита — через WASM в браузере.

### Только сервер

```powershell
cd server
python -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt
uvicorn main:app --reload --port 8000
```

Health: `http://localhost:8000/api/health`  
Подробности API: [server/README.md](server/README.md).

---

## Сборка фронта для продакшена

```bash
cd viewer
npm run build
```

Результат: каталог **`viewer/dist/`** — статика для nginx/IIS или любого static host.

---

## Структура репозитория

| Путь | Назначение |
|------|------------|
| `viewer/` | Vue-приложение: `App.vue` (вкладки, отчёт), `components/Viewer3D.vue` (сцена, PDF, сборка, измерения, сечение), `lib/stepLoader.ts` и др. |
| `server/` | FastAPI: метаданные STEP, конвертация STEP→GLB, коллаборация/чат и вспомогательные API |
| `run.bat` | Точка входа «поднять всё локально» (если присутствует в вашей копии) |

Каталог **`icons/`** можно использовать для ресурсов интерфейса; в репозиторий имеет смысл класть только нужные файлы без тяжёлых тестовых наборов.

---

## Ограничения и ожидания

- Проект ориентирован на **инженерный обмен** и **внутреннее** использование; публичный коммерческий релиз требует отдельной проработки лицензий (OCCT, PDF.js, шрифты и т.д.).
- Тяжёлые сборки STEP: разумные лимиты размера файла, терпение при первой WASM-загрузке, по возможности — серверная конвертация для ускорения.

---

## Идеи развития (дорожная карта)

- **PWA** — установка с домашнего экрана, удобные ссылки/QR без магазина приложений.
- **Каналы вроде Teams** — доставка как **ссылка** на задеплоенный вьювер.
- **Облачное хранилище** (Яндекс.Диск и аналоги) — единая папка проекта рядом со ссылкой на вьювер.

## План развития (зафиксировано)

### Этап 1 — фундамент совместной работы
- БД (PostgreSQL): `users`, `projects`, `project_members`, `chat_channels`, `chat_messages`, `chat_attachments`, `message_reads`, `audit_log`.
- Авторизация: JWT access/refresh, роли `owner/editor/viewer`, проверка membership на API и realtime.
- API-каркас: `/auth/*`, `/projects/*`, `/chat/*`, базовые health/status.
- Миграции и эксплуатация: Alembic/Flyway, backup/restore, базовые логи и метрики.

### Этап 2 — связка PDF и 3D в проекте
- Реестр проекта (`project_index`) и автосопоставление PDF ↔ 3D по правилам именования.
- Ручная привязка/перепривязка файлов в UI.
- Открытие связанной пары в 1-2 клика.

### Этап 3 — чат с контекстом
- Вложения скриншотов из PDF и 3D.
- Контекст сообщения: страница PDF, камера/деталь 3D, файл проекта.
- Realtime-доставка сообщений и статусы прочтения.

### Этап 4 — QR и мобильная страница модуля
- Уникальные QR на чертеж/лист в рамках проекта и модуля.
- Перегенерация и перезапись QR при ошибке или изменении источника.
- Mobile-first страница для производства: объект, проект, модуль, профиль, габариты, место хранения, ссылка на чертеж (через PDFViewer).

### Этап 5 — интеграция с Яндекс.Диском
- Синхронизация с множеством корневых папок по модулям.
- Инкрементальное обновление по etag/hash/mtime.
- Статусы синка и обработка конфликтов.

---

## Репозиторий и контакты

| | |
|--|--|
| **GitHub** | [https://github.com/AlexPror/3D_viewer](https://github.com/AlexPror/3D_viewer) |
| **E-mail** | [vorobjev.alexandr-2017@yandex.ru](mailto:vorobjev.alexandr-2017@yandex.ru) |

На странице репозитория в GitHub можно заполнить поле **About** (описание, сайт, topics) — тогда ссылка и краткое назначение проекта будут видны сразу под заголовком, как в [карточке репозитория](https://github.com/AlexPror/3D_viewer).

---

## Лицензия

Внутренний инструмент; **не заявлен как готовый публичный коммерческий продукт** без отдельного решения по лицензированию зависимостей и контента.
